# Kopiera till .env och fyll i värden
OPENAI_API_KEY=replace-with-your-key

# Realtime transcribe-modell (OpenAI)
# Exempel: gpt-4o-mini-transcribe eller gpt-4o-transcribe
OPENAI_REALTIME_MODEL=gpt-4o-mini-transcribe

# Språk (ISO-kod). 'sv' = svenska
LANGUAGE=sv

# Samplingsfrekvens för PCM16
SAMPLE_RATE=16000

# CORS – tillåtna origins (kommaseparerat). Tomt => defaultlista + regex för *.lovable.app
ALLOWED_ORIGINS=

# Ringbufferstorlek per debuglista
RING_SIZE=200
